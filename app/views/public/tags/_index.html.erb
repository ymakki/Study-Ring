<table class="table table-hover">
  <% tags.each do |tag| %>
    <tr onclick="location.href='<%= edit_user_tag_path(user_id: tag.user_id) %>'" style="cursor:pointer;">
      <td class="break-word">
        <%= tag.name %>
      </td>
    </tr>
  <% end %>
</table>