<table class="table">
  <% study_reviews.each do |study_review| %>
    <tr>
      <td class="d-flex mb-3 mr-2">
        <div class="icon mr-3">
          <% if study_review.user.profile_image.attached? %>
            <%= image_tag study_review.user.get_profile_image(70, 70), class: "img-fluid user-image" %>
          <% else %>
            <i class="fa-regular fa-circle-user"></i>
          <% end %>
        </div>
        <div class="flex-column">
          <div class="text-size"><%= study_review.user.name %></div>
          <div class="text-size break-word"><%= study_review.title %></div>
          <div class="text-size break-word"><%= study_review.body %></div>
        </div>
        <div class="ml-auto text-size">
          <%= link_to '削除', admin_study_review_path(study_review), method: :delete, data: { confirm: "このレビューを削除しますか？" }, class: "btn btn-danger" %>
        </div>
      </td>
    </tr>
  <% end %>
</table>
