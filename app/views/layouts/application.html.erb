<!DOCTYPE html>
<html>
  <head>
    <title>StudyRing</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <script src="https://kit.fontawesome.com/8c75a932ab.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column h-100" style="background-image: url('<%= asset_path('background.png') %>'); background-size: cover;">
    <header>
      <% if request.fullpath.include? "/admin" %>
        <%= render "layouts/admin_header" %>
      <% else %>
        <%= render "layouts/public_header" %>
      <% end %>
    </header>
    <main class="flex-shrink-0 wrapper">
      <%= flash[:notice] %>
      <div class="container">
        <div class='row pb-5'>
          <div class="col-md-8 mx-auto pt-2">
            <%= yield %>
          </div>
        </div>
      </div>
    </main>
    <footer>
      <div class="text">
        Copyright © Infratop Inc. All Rights Reserved.
      </div>
    </footer>
  </body>
</html>
